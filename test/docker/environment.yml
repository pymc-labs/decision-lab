# Installed into base conda env (no named env needed)
channels:
  - conda-forge
dependencies:
  - python=3.11
  # Add your conda packages here
  # - numpy
  # - pandas
  - pip:
    - dhub-cli
    - modal
